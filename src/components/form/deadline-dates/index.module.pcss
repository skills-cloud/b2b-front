.deadline-dates {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    grid-gap: 4px;
    align-items: flex-start;
}

.deadline-dates__separator {
    --color: #d6d9de;
    width: 16px;
    height: 1px;
    background: var(--color);
    margin-top: 43px;
}
